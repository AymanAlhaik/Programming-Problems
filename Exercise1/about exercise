In a plane N points are given (N < 100,000). The coordinates of the points are integers (xi, yi),
Write a program, which finds all horizontal and vertical lines, such that they split the plane into two parts,
each containing an equal set of points (points lying on the line are note counted)
